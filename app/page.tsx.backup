'use client'

import { motion } from 'framer-motion'
import { Sparkles, Video, Zap } from 'lucide-react'
import Link from 'next/link'

export default function HomePage() {
  return (
    <div className="relative min-h-screen overflow-hidden bg-black">
      {/* Video Background - Positioned to show face */}
      <div className="absolute inset-0 z-0">
        <video
          autoPlay
          loop
          muted
          playsInline
          className="h-full w-full object-cover object-top"
          style={{ 
            objectPosition: 'center 20%', // Show face prominently
            filter: 'brightness(0.7)' // Slight darken for text readability
          }}
        >
          <source src="/videos/user-ai-generation-FomhdaM140Cu-1080p.mp4" type="video/mp4" />
        </video>
        
        {/* Gradient overlay - only on bottom half */}
        <div className="absolute inset-0 bg-gradient-to-b from-transparent via-transparent to-black/90" />
      </div>

      {/* Content - Pushed to bottom */}
      <div className="relative z-10 flex min-h-screen flex-col">
        {/* Spacer to push content down - KEEPS FACE VISIBLE */}
        <div className="flex-grow" style={{ minHeight: '60vh' }} />
        
        {/* Text content - Bottom third only */}
        <div className="px-6 pb-16 pt-8 sm:px-8 lg:px-16">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            className="max-w-4xl"
          >
            <h1 className="mb-6 text-5xl font-bold leading-tight text-white sm:text-6xl lg:text-7xl">
              Create Stunning
              <br />
              <span className="bg-gradient-to-r from-purple-400 via-pink-500 to-orange-500 bg-clip-text text-transparent">
                AI-Powered Videos
              </span>
            </h1>

            <p className="mb-8 max-w-2xl text-lg text-gray-300 sm:text-xl">
              Transform your ideas into captivating video content with our AI-powered creative workspace.
            </p>

            <div className="flex flex-wrap gap-4">
              <Link
                href="/workspace"
                className="group relative inline-flex items-center gap-2 overflow-hidden rounded-full bg-white px-8 py-4 text-lg font-semibold text-black transition-all hover:scale-105"
              >
                <Sparkles className="h-5 w-5" />
                Start Creating
                <div className="absolute inset-0 -z-10 bg-gradient-to-r from-purple-600 to-pink-600 opacity-0 transition-opacity group-hover:opacity-100" />
              </Link>

              <Link
                href="/about"
                className="inline-flex items-center gap-2 rounded-full border-2 border-white/30 bg-white/10 px-8 py-4 text-lg font-semibold text-white backdrop-blur-sm transition-all hover:border-white/50 hover:bg-white/20"
              >
                <Video className="h-5 w-5" />
                Watch Demo
              </Link>
            </div>
          </motion.div>

          {/* Feature badges */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.3 }}
            className="mt-12 flex flex-wrap gap-6"
          >
            {[
              { icon: Zap, text: 'AI-Powered' },
              { icon: Video, text: 'HD Quality' },
              { icon: Sparkles, text: 'Easy to Use' },
            ].map((feature, i) => (
              <div
                key={i}
                className="flex items-center gap-2 rounded-full border border-white/20 bg-white/5 px-4 py-2 backdrop-blur-sm"
              >
                <feature.icon className="h-4 w-4 text-purple-400" />
                <span className="text-sm text-gray-300">{feature.text}</span>
              </div>
            ))}
          </motion.div>
        </div>
      </div>
    </div>
  )
}
