import { NextResponse } from 'next/server';

export async function POST(request: Request) {
  // Placeholder endpoint for AI hooks
  return NextResponse.json({ 
    status: 'ok',
    message: 'Create endpoint placeholder'
  });
}
